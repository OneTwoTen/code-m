name: CI

on:
  push:
    branches: [main, "docs/architecture-bun-mcp"]
  pull_request:

permissions:
  contents: read

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.3

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Format changed sources
        run: bunx biome format --write .

      - name: Upload formatted sources
        uses: actions/upload-artifact@v4
        with:
          name: formatted-review-fixes
          path: |
            apps/mcp-server/src/github/github-app.ts
            apps/mcp-server/src/github/github-app.test.ts
            apps/mcp-server/src/workspace/git-transport.ts
            apps/mcp-server/src/workspace/process-git-transport.ts
            apps/mcp-server/src/workspace/repository-workspace-service.ts

      - name: Check formatting
        run: bun run format:check

      - name: Lint
        run: bun run lint

      - name: Typecheck
        id: typecheck
        shell: bash
        run: |
          set -o pipefail
          bun run typecheck 2>&1 | tee typecheck.log

      - name: Upload typecheck log
        if: failure() && steps.typecheck.outcome == 'failure'
        uses: actions/upload-artifact@v4
        with:
          name: typecheck-log
          path: typecheck.log

      - name: Test
        id: test
        shell: bash
        run: |
          set -o pipefail
          bun run test 2>&1 | tee test.log

      - name: Upload test log
        if: failure() && steps.test.outcome == 'failure'
        uses: actions/upload-artifact@v4
        with:
          name: test-log
          path: test.log

      - name: Build Docker image
        run: docker build --tag codem-mcp:ci .
